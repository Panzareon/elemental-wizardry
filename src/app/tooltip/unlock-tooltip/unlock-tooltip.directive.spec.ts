import { UnlockTooltipDirective } from './unlock-tooltip.directive';

describe('UnlockTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlockTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
