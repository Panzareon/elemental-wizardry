import { BuffTooltipDirective } from './buff-tooltip.directive';

describe('BuffTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new BuffTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
