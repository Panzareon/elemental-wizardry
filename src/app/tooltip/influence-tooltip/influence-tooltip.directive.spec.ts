import { InfluenceTooltipDirective } from './influence-tooltip.directive';

describe('InfluenceTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new InfluenceTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
