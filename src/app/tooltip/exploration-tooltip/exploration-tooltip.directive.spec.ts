import { ExplorationTooltipDirective } from './exploration-tooltip.directive';

describe('ExplorationTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplorationTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
